#!/bin/sh
set -eu

case "${SITEHITS_MCP_IMAGE_REF:-}" in
  ghcr.io/onurmatik/site-hits@sha256:????????????????????????????????????????????????????????????????)
    digest=${SITEHITS_MCP_IMAGE_REF##*@sha256:}
    case "$digest" in
      *[!0-9a-f]*)
        echo "SITEHITS_MCP_IMAGE_REF digest must be lowercase hexadecimal" >&2
        exit 78
        ;;
    esac
    ;;
  *)
    echo "SITEHITS_MCP_IMAGE_REF must pin ghcr.io/onurmatik/site-hits by sha256 digest" >&2
    exit 78
    ;;
esac
